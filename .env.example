# .env.example — Modelo de variáveis de ambiente para o Painel ZapPRO

# Porta padrão do painel
PORT=8000

# Token de segurança padrão (exemplo — deve ser alterado!)
SECRET_KEY=exemplo_token_seguro

# Modo do servidor (dev ou prod)
APP_ENV=development

# Domínio do painel
BASE_URL=http://localhost:8000
